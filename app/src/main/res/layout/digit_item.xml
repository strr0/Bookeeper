<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="@dimen/dashboard_square_size"
        android:layout_height="@dimen/dashboard_square_size"
        android:layout_marginVertical="@dimen/dashboard_square_margin"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackground"
        app:cardBackgroundColor="@color/dashboard_item_bg"
        app:cardCornerRadius="8dp"
        app:cardElevation="1dp"
        app:strokeColor="@color/dashboard_item_border_default"
        app:strokeWidth="@dimen/dashboard_square_stroke">

    <TextView
            android:id="@+id/digit_value"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:textColor="@color/common_text_primary"
            android:textSize="16sp"
            android:textStyle="bold"/>
</com.google.android.material.card.MaterialCardView>
